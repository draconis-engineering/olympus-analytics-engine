[package]
name = "oae-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
oae-core = { path = "../oae-core" }
rusqlite = { version = "0.40", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
